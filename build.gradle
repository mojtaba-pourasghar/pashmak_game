// Top-level build file. Module-level config lives in app/build.gradle.
plugins {
    id 'com.android.application' version '8.6.1' apply false
}

tasks.register('clean', Delete) {
    delete rootProject.buildDir
}
