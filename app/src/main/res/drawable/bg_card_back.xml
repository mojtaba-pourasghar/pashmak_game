<?xml version="1.0" encoding="utf-8"?>
<!-- Every face-down card looks identical, so nothing leaks about its pair. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/white" />
            <stroke android:width="2dp" android:color="@color/memory_back_ink" />
            <corners android:radius="18dp" />
        </shape>
    </item>
    <item
        android:bottom="18dp"
        android:left="18dp"
        android:right="18dp"
        android:top="18dp">
        <shape android:shape="oval">
            <solid android:color="@color/memory_back_ink" />
        </shape>
    </item>
</layer-list>
